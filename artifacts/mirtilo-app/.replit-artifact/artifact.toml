kind = "web"
previewPath = "/"
title = "Pesagem de Mirtilo"
version = "1.0.0"
id = "artifacts/mirtilo-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19657

[services.development]
run = "pnpm --filter @workspace/mirtilo-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mirtilo-app", "run", "build" ]
publicDir = "artifacts/mirtilo-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19657"
BASE_PATH = "/"
